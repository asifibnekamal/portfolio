import resume from '../../Image/resume.png';
const Resume = () => {
    return (
        <div className='flex py-9'>
            <img src={resume} alt="" className="max-w-sm m-auto shadow-2xl" />
        </div>
    );
};

export default Resume;